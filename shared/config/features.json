{
  "features": {
    "login": {
      "path": "./features/login",
      "description": "Authentication (planned; not wired in MVP)"
    },
    "dashboard": {
      "path": "./features/dashboard",
      "description": "Dashboard UI + REST handlers (Next.js App Router)"
    }
  },
  "nextApp": {
    "devUrl": "http://localhost:3000",
    "note": "Single Next.js process serves pages and /api/*"
  }
}
