import React from 'react'

export const ResponsiveLine = (props: any) => (
  <div data-testid="mocked-line-chart" {...props}>
    Mocked Line Chart
  </div>
)

export const ResponsiveBar = (props: any) => (
  <div data-testid="mocked-bar-chart" {...props}>
    Mocked Bar Chart
  </div>
)
