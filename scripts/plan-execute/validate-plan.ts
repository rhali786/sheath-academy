/**
 * Backward-compatible entry point for npm scripts and imports.
 * Implementation lives in the portable skill bundle.
 */

export {
  validatePlanBundle,
  type ValidatePlanOptions,
  type ValidationReport,
} from '../../.claude/skills/validate-plan/validate-plan'
